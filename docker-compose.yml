version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=sqlite:///./neuroops.db

  frontend:
    build: ./frontend-app
    ports:
      - "5174:5174"
    depends_on:
      - backend
